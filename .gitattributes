# Unified diffs require a leading context-space on otherwise blank lines.
upstream/*.patch text eol=lf -whitespace
# Preserve the supplied driver's license bytes and recognize its CRLF endings.
licenses/Wintun-Binary-LICENSE.txt whitespace=cr-at-eol
